import { ComingSoon } from '@/components/site/coming-soon';

export const metadata = { title: 'Returns' };

export default function Page() {
  return <ComingSoon title="Returns" subtitle="14-day UK returns. Full policy — Phase 3." />;
}
